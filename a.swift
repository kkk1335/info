// 一、必须优先评估的“硬影响”
// 1.
// TLS 最低版本上调到 1.2（URLSession 默认）
// 影响：所有通过 URLSession 的 HTTPS 连接，必须保证后端至少支持 TLS 1.2（最好 1.3）。老旧反向代理/中间件或历史子域若仍停留在 TLS 1.0/1.1 会直接失败。
// 行动：
// 2.
// 
// 对所有域名做一次 TLS 扫描，确保协商≥1.2；合规上也更符合金融行业安全基线。
// 
// 
// 若你有自定义 nw_protocol_options 的地方，确认没有强行降级。
// 依据：
// 
// 1.
// 日志隐私：NSLog 在统一日志系统中会“自动打码”动态字符串
// 影响：你在排查线上问题时，Console/日志聚合里看到的生产日志可能被红acted（避免意外泄露PII）。Xcode 调试台的实时输出不受影响，但统一日志/持久化里会被处理。
// 行动：
// 2.
// 
// 对关键路径改用 Logger（os log）并用结构化字段；对必须明文的极少量信息（避免包含个人数据）使用相应选项并明确合规策略。
// 依据：
// 
// 1.
// Core Data：iCloud Ubiquitous 相关键位被弃用且重新编译会报错
// 影响：如果你历史上用过老的“文稿级 iCloud 同步键”（非 CloudKit/非 NSPersistentCloudKitContainer），用 iOS 26 SDK 重新编译会直接出错。
// 行动：
// 2.
// 
// 全面迁移到 NSPersistentCloudKitContainer 或 SwiftData；删除任何 NSPersistentStoreUbiquitous* 相关配置。
// 依据：
// 
// 1.
// UIKit 搜索相关“已知问题”会影响常见列表检索场景
// 2.
// 
// UISearchBar.searchTextField 在某些情况下可能返回不同实例（破坏对引用的假设）。
// 
// 
// 放在 UIToolbar 的搜索栏可能点击无响应；官方建议在导航栏的 Search Bar 中禁用工具栏整合。
// 行动（临时规避）：
// 
// // iOS 26：避免 toolbar 集成，减少交互异常if #available(iOS 26, *) {
//     navigationItem.searchController?.searchBarPlacementAllowsToolbarIntegration = false
// }
// 
// 尽量通过 UISearchController/导航栏集成的标准路径，而不是把 search bar 手动塞进 UIToolbar。
// 依据：
// 
// 1.
// UIScreen.main 被弃用
// 影响：常见的窗口/屏幕获取代码需要调整，否则会出现弃用告警，后续可能影响多窗口/外接屏场景。
// 行动（替代）：
// 2.
// // 在视图/控制器上下文中let screen = view.window?.windowScene?.screen// 或在应用级let anyScene = UIApplication.shared.connectedScenes.first as? UIWindowScenelet screen2 = anyScene?.screen
// 依据：
//
// 二、UI/交互与国际化的变更（需要专项回归）
// 1.
// SwiftUI：文字排版与书写方向默认值调整
// 2.
// 
// 每段落的书写方向/对齐有更精细的默认规则，阿拉伯文/希伯来文等 RTL 文本在列表、表单、按钮上的表现可能与 iOS 17–18 不同。
// 
// 
// Text 的字符串拼接用 + 已弃用，推荐插值或富文本拼接（便于本地化&无障碍）。
// 行动：
// 
// 
// 对阿语/希伯来语等语言做一次专项 UI 回归（表单、交易明细列表、分段控件、导航标题）。
// 
// 
// 全局搜索 Text(a + b) 改为 Text("\(a)\(b)") 或使用 Text 的组合 API。
// 依据：
// 
// 1.
// TextKit：列表标记参与分词的默认行为改变
// 影响：如果你做站内搜索/高亮并依赖 NSTextList 的标记参与文本分词，iOS 26 默认不再包含这些标记。
// 行动：审查自定义分词/搜索高亮逻辑，必要时启用包含列表标记的选项。
// 依据：
// 2.
// 3.
// SwiftUI 控件尺寸与导航联动
// 4.
// 
// Segment/Picker/Button 的大尺寸与布局行为有细节变化；NavigationLink 在容器环境值上的变化可能影响你依赖环境驱动的导航流。
// 行动：检查大型按钮/分段控件的可达性与点击区域；检查依赖 containerValues 的自定义导航逻辑。
// 依据：
// 
// 1.
// 地图手势回调潜在崩溃
// Map 的 onCameraChange 在复用期间可能触发回调导致崩溃（已列为已知问题/修复项变动过）。
// 行动：在回调中避免强引用 UI 状态，增加保护（例如异步去抖或弱引用 self）。
// 依据：
// 2.
//
// 三、与“风控/合规/安全”相关的注意点
// 1.
// VPN/IKEv2 弃用老旧算法（若你们企业版内置 VPN 或企业分发用）
// DES/3DES、SHA1-96/160、弱 DH 组（<14）等在 IKEv2 中不再受支持。
// 行动：运维侧更新 IKEv2 配置到强算法族（AES-GCM、SHA2、DH14+）。
// 依据：
// 2.
// 3.
// Core Data 并发一致性检查的调试默认值
// 一些“放宽 Sendable”的调试开关默认被关掉，有助于尽早暴露数据竞争。
// 行动：在测试版/灰度中打开并发检测，修正所有警告；对金融数据模型尤其重要。
// 依据：
// 4.
// 5.
// 通知图标跟随外观的行为提醒
// 系统通知图标不会自动跟随外观变更（黑/白样式），不要依赖系统自动反转。
// 行动：按需提供适配良好的图标资源。
// 依据：
// 6.
//
// 四、与“增长/运营”相关的变更（看团队需求）
// 
// App Store：产品页支持“无障碍标签”（描述/推广文本加辅助标签），利于可达性与审核。
// 行动：更新产品页元数据，添加可达性标签文案。
// 依据：
// 
// 
// AdAttributionKit 更新（窗口与冷却期）
// 如果你们做投放归因，需要同步 SDK/配置以匹配新的窗口与 cooldown 逻辑。
// 依据：
// 
//
// 五、照片/文件选取相关（KYC/上传证件的常见流程）
// 
// 照片搜索/相册选择的若干“已知问题/修复”：iOS 26 的 Photos/PhotosUI 在搜索与结果排序上有过变动和修复记录。
// 行动：
// 
// o
// 若你在开户/KYC 用 PHPickerViewController 或自建相册选择器，回归测试搜索、最近项目、权限请求多语言提示。
// 依据：
// o
//
// 六、迁移/回归测试清单（给团队的直接待办）
// A. 代码层面改造
// 
// 全库搜：UIScreen.main → 场景感知的 windowScene?.screen。
// 
// 
// 全库搜：Text(a + b) → 插值或富文本组合。
// 
// 
// URLSession/网络层：确认 TLS ≥ 1.2；CI 加入 TLS 扫描脚本。
// 
// 
// 日志：从 NSLog 迁移到结构化 Logger，建立“PII 不落日志”的校验规则。
// 
// 
// Core Data：排查并移除 Ubiquitous 相关键；（如需云同步）统一到 CloudKit/SwiftData。
// 
// 
// 搜索：如果使用自定义分词/高亮，核对 TextKit 的列表标记参与策略。
// 
// 
// 搜索栏整合：禁用 searchBarPlacementAllowsToolbarIntegration 以绕过交互问题。
// 
// B. QA 回归重点
// 
// 语言/排版：阿语/希伯来语、泰语等复杂脚本的表单、对齐、列表和按钮点击区域。
// 
// 
// 网络降级路径：弱网/重定向/证书轮换日；TLS 协商失败时的用户提示与重试策略。
// 
// 
// 搜索体验：带工具栏的搜索栏交互、UISearchBar.searchTextField 引用稳定性。
// 
// 
// 媒体上传：相册选择器在多语言和权限边界上的表现。
// 
// 
// 日志巡检：验证生产日志确实不会落 PII，且仍能支撑故障排查（对关键埋点做一次演练）。
// 
// C. 安全/合规联动
// 
// 后端/网关团队：统一升级 TLS；合规文档记录“最低 TLS 版本变更”。
// 
// 
// （若涉及企业 VPN）确认 IKEv2 配置已替换不安全算法。
// 
//
// 七、可能“诱人但需谨慎”的新东西
// 
// Apple Intelligence / Foundation Models
// 如果你们考虑端侧智能助理或对话功能：需关注设备与地区可用性、隐私合规、Prompt/日志处理、离线策略；银行卡号/身份证等绝不可进入模型上下文。文档里只标注了框架可用与若干修复，产品落地前建议单独做“数据出境/AI 合规评估”。
// 
//
// TL;DR（给忙同事看的）
// 
// 立刻做：TLS≥1.2 全域核查、NSLog→结构化 Logger、剔除 Core Data 旧 iCloud 键、禁用 SearchBar-Toolbar 整合、替换 UIScreen.main。
// 
// 
// 回归做：RTL 语言 UI、搜索/相册/地图回调、网络降级与错误提示、日志不含 PII。
// 
